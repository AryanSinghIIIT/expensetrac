import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  income: {
    borderBottom: '20px solid rgba(0, 255, 0, 0.8)',
    borderRadius: '15px',
  },
  expense: {
    borderBottom: '20px solid rgba(255, 0, 0, 0.8)',
    borderRadius: '15px',

  },
  
}));
